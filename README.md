# color_detection
An interactive Python project using OpenCV and Pandas to detect and display color names from images. Users can double-click any pixel to instantly view the closest matching color along with RGB values. The application reads color datasets from CSV files and provides real-time visual feedback through a simple graphical interface for beginners too!!
